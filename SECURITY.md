# Security Policy

Do not report suspected vulnerabilities in a public issue. Send a private report to the repository maintainer through the GitHub security advisory flow, including reproduction steps and sanitized logs.

Never include API keys, OAuth tokens, `~/.kimi-code` credentials, `.claude` state, or unredacted task logs in an issue or pull request.
